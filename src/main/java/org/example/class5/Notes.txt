Scanner class:
   Scanner is a class that helps us take input data from
   the user from keyboard. Just like scanner class we have so
   may classes that we can use from Java and other libraries.
   These classes are divided into methods each method can do useful.
   Like nextInt(),nextBoolean(), nexDouble()/

Logical operators:
   Logical operators are used to combine multiple conditions.
   There are three logical operators in Java:
   1. Logical AND "&&"
   2. Logical OR "||"
   3. Logical NOT "!"

Switch statements:


Important Note:Every class in Java also acts as a data type.
   We can created variable of these classes.
   if we want to reuse some codes from the class, we can create
   its objects and than we can start calling those methods.